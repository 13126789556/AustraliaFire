using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class desertTile : MonoBehaviour
{
    /*
    public enum forestStateList {fire, notFire};
    public forestStateList forestState;
    private void OnMouseDown()
    {
        if (forestState == blockTypeList.forest && GM.curAction == GameManager.actionList.fightFire)
        {
            //this.GetComponent<SpriteRenderer>().color = Color.yellow;
            print("start fighting fire");
        }
        if (GM.curAction == GameManager.actionList.cleanWater)
        {
            //this.GetComponent<SpriteRenderer>().color = Color.blue;
            print("start cleaning water");
        }
    }
    */
}
